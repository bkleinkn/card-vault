{
  "name": "card-vault-functions",
  "description": "Cloud Functions for Card Vault — Claude-powered card identification.",
  "version": "0.1.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint .",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^10.5.0"
  }
}
